#include <gmock/gmock.h>
#include <map>

#include "../map/s21_map.cc"

using namespace std;

TEST(TestGroupName, map) {
    // S21Queue<int> q{3, 5, 2, 1, 6};
    // S21Queue<int> check(move(q));

    // ASSERT_TRUE((q != check) == true);
    ASSERT_TRUE(0 == 0);
}

